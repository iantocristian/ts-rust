[package]
name = "ts_arena"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Checked owner and arena ids, lazy storage, bundle ownership and validated scopes: the ADR 0006 contract"

[lints]
workspace = true

[dependencies]
parking_lot = "0.12"
