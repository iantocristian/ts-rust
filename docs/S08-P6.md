# S08 P6 — Semantic acceptance

The [E2 acceptance producer](S08-E2.md) is wired before the full corpus run.
It consumes authenticated captures, includes public `TypeToString` observations
and executes the comparator/recursion obligations. Bounded smoke results do not
advance the acceptance denominator or establish completion of P6.

P6 starts on `codex/s08-p6`, based on P5 commit `5994198` and PR #23.
Its acceptance inventory remains 9,369 variants, with 1,359 informational
variants reported separately. Supplemental selections do not change either
denominator or establish a full-corpus pass count.

## Implementation sequence

1. Use the retained P5 failures to select a concrete semantic family. Compare
   the checker path with pinned Go, port the missing behavior through the
   existing owner and type stores, and add source-selected native witnesses.
   Preserve raw differences rather than changing the comparator to accept them.
2. Recheck affected frozen variants together with previously matching controls.
   Record diagnostic bytes, type/symbol bytes and actual query schedules
   separately. Keep execution failures distinct from completed-but-different
   results. A subset improvement does not update the historical full count.
3. Resolve remaining diagnostic, relation and display families from those
   comparisons. With the E2 capture/verification layer in place, run one
   complete inventory after the implementation stabilizes;
   use bounded rechecks while developing. Review diagnostic order and arguments,
   query side effects, recursive behavior and cache ownership independently.
4. Connect the exact semantic comparisons to E2 with source-bound evidence;
   retain the native runner's phase selection and the S07-3 amendment. Complete
   the S08 ownership contributors and applicable E3 instrumentation, including
   failure/reentry, retained handles and generation retirement. Audit recursion
   and approved divergences before claiming the nonmeasurement requirements.
5. Leave checkerbench, relater and footprint acceptance to P7, and the final
   coordinated producer refresh to P8. Neither a supplemental comparison nor
   a successful diagnostic execution marks E2 or S08 complete.

## Dynamic import checking

The first increment ports `checkImportCallExpression` and its grammar checker.
Previously expression checking sent `import(...)` through ordinary untyped
call resolution, returning `any`. It now checks every argument in native order,
resolves the module with its attributes, applies the existing default-only or
synthetic-default namespace wrapper, and returns `Promise<namespace>`.
Nonliteral and unresolved imports return `Promise<any>` with the appropriate
diagnostics. Missing Promise declarations use the import-specific messages.

The contextual argument path supplies `string`, `ImportCallOptions` and `any`
for the first, second and surplus arguments. Reporting and nonreporting global
lookups have separate caches, as in Go; both are included in retained-type
traversal. Quick expression checks, import-attribute extraction and inline
attribute const contexts now recognize the complete `IsImportCall` predicate,
including `import.defer`.

The grammar port preserves early returns for module-mode restrictions, type
arguments, the second argument in older modes, invalid arity and spread
arguments. Semantic checks still visit excess arguments. Parenthesized options
preserve the native attribute behavior, while the obsolete `assert` diagnostic
only inspects an original object-literal argument.

This uses the existing module resolver, namespace wrappers, Promise machinery,
owner and paged type storage. It adds no dependency, alternate checker path,
unsafe code or relaxed acceptance rule.

## Focused verification

Eleven source-selected programs provide 59 top-level public type queries and
complete semantic diagnostic payloads. They cover awaited namespaces,
attribute-discriminated pattern modules, JSON default-only exports, CommonJS
namespaces, invalid/nonliteral specifiers, argument grammar, obsolete assertions,
deferred imports with attributes, and absent Promise types or constructors.
The Rust integration test compares them with native observations and repeats
each query on the same checker owner.

The full P2 comparison additionally observes nested properties and signatures.
It matches 3/11 programs; 8 differ only in nested display fields, including
truncated `Promise.then` signatures. Those differences remain failures; the
narrower regression projection does not certify the full comparison. The
missing-global fixtures match the full P2 comparison. Native requests,
observations, sources and the fixture projection are retained in the
[P6 evidence](../data/s08/p6/README.md).

The bounded corpus recheck improves from 15 to 19 exact matches among its 22
acceptance variants; one informational variant still differs. All previous
matches remain matches, all 23 executions complete, and the captured sources
are stable. It fixes `importAttributeDynamicImports` and the three selected
`nodeModulesDeclarationEmitDynamicImportWithPackageExports` configurations.
The remaining selected differences belong to the `esmModuleExports` family.

The new regression test, clippy for checker/compiler across all targets and
features with warnings denied, the checkertext producer, formatting and ledger
validation pass. The S07 operation mappings remain unchanged. Full E2
acceptance, complete-corpus remeasurement, E3 instrumentation and P7 performance
measurements remain outstanding. The temporary CI runner selection and all
thresholds are unchanged.

## JavaScript syntactic diagnostics and JSDoc variadic operands

`Program::syntactic_diagnostics` now combines parser and JavaScript grammar
diagnostics, includes the option-dependent parameter-decorator check for
unchecked JavaScript, and sorts and deduplicates through the program collector.
File-level `@ts-check` and `@ts-nocheck` directives override `checkJs`, as in
pinned Go. The P2 and corpus adapters use this API. Content-map span filtering
remains an explicit unsupported boundary rather than silently retaining a
diagnostic that Go could discard.

Both the type-node reuse path and declaration transformer now read the operand
from `JSDocVariadicType.Type`. The generic node accessor remains unchanged.
The transformer still records the original node after creating its array type.
Regression coverage includes native baseline displays for rest parameters and
a variadic typedef transformed into `number[]`. The typedef expectation comes
from the pinned parser, reparser and transformer code; it is not a recorded
native baseline result.

The full development capture at `target/s08/e2-jsdiag` completed all 10,728
variants with stable sources. Its corpus capture SHA-256 is
`81ec2f390b7d5a244d88eacc0ee0f96a4fe4c99de2abdc458cec88fcfdfd857a`.
Comparing its verified rows with the `730abb2` capture gives:

| Acceptance result | `730abb2` | JS diagnostics and display fix |
|---|---:|---:|
| Types parity | 0.9692603 | 0.9705411 |
| Errors parity | 0.9449248 | 0.9507952 |
| Public type-to-string parity | 0.9768385 | 0.9772654 |
| Types and errors both pass | 8,672 | 8,731 |
| All three domains pass | 8,664 | 8,723 |

No previously accepted comparison regressed in either tier. All four JSDoc
rest-parameter failures now match in every domain. The acceptance denominator
remains 9,369; one variant still fails execution, and E2 does not pass.

This capture predates the separate declaration-transformer fix and its test.
It is retained as a development comparison, not recorded as current E2
evidence. Review also refreshed three operation-inventory source locations
shifted by the diagnostic collector edits. The earlier `730abb2` E2 evidence
is preserved, and the status views mark it stale for the changed sources.

The implementation report records clean workspace formatting and clippy,
688 passing Rust tests in both profiles, and 442 passing script tests before
the separate transformer increment. The review reused those checks, inspected
the pin and regression coverage, and independently compared the saved corpus
rows and source fingerprints; it did not repeat the full capture or suites.

## Skipped grammar diagnostics, enum member parents, missing names and write access

TS1216 for an exported `__esModule` binding now goes through a port of
`grammarErrorOnNodeSkippedOnNoEmit`. The checker reports it with
`skipped_on_no_emit` regardless of `noEmit`, and program semantic selection
removes it under `noEmit`, as in pinned Go. The previous checker-level `noEmit`
gate is gone. The regression uses the `compiler/es5-commonjs8.ts` source.

Enum member display now ports `appendReferenceToType` and `getAccessStack`.
When the parent enum is reachable only through an import type, as in
`compiler/declarationEmitQualifiedName.ts`, the member extends the import
qualifier. Quoted members use a `typeof` import type with an indexed access.
A Rust-only approximate-length increment was removed. The instantiation
expression and access-expression branches of `appendReferenceToType` remain
named unsupported boundaries; the enum caller cannot reach them. The native
witness is `data/s08/p6/enum-member-display`.

`getResolvedSymbol` no longer resolves a parser-inserted missing identifier.
Focused P2 witnesses in `data/s08/p6/missing-identifier` show equal syntactic
diagnostics before the change, so parser recovery was not the cause; only the
checker added `Cannot find name '(Missing)'`.

`accessKind`, `IsWriteAccess` and `IsWriteOnlyAccess` are now ported. They
supply the name-resolution `isUse` flag, the write-only exemption in
`markPropertyAsReferenced`, the accessibility writing flag and write-type
selection, replacing assignment-target approximations. Regressions use the
pinned `compiler/noUnusedLocals_writeOnly.ts` baseline and native witnesses in
`data/s08/p6/write-access`, captured with the new program-diagnostics driver
`tools/s08/p6/semantic_diagnostics_oracle_test.go`.

A full capture of these changes is at `target/s08/e2`, corpus capture SHA-256
`6b7f7a19e2f56fe634364c03fa7354c28b0f3ec43a4788ed9f33820b869d637d`. The
`730abb2` capture was renamed to `target/s08/e2-730abb2`. Native observations
are byte-identical across all three captures.

| Acceptance result | `730abb2` | JS diagnostics (`e2-jsdiag`) | This batch |
|---|---:|---:|---:|
| Types parity | 0.9692603 | 0.9705411 | 0.9706479 |
| Errors parity | 0.9449248 | 0.9507952 | 0.9673391 |
| Public type-to-string parity | 0.9768385 | 0.9772654 | 0.9773722 |
| Types and errors both pass | 8,672 | 8,731 | 8,881 |
| All three domains pass | 8,664 | 8,723 | 8,873 |
| Execution failures | 5 | 1 | 0 |

Against `e2-jsdiag`, 155 acceptance error comparisons now match:
- 130 lost only Rust-only `(Missing)` diagnostics;
- 14 gained native unused-declaration diagnostics (13 TS6133 and one TS6199);
- 10 differed only in the TS1216 skip flag;
- `declarationEmitQualifiedName` completes and matches in all three domains.

Types and type-to-string each gained that one variant. Seven informational error
comparisons also match. No previously matching comparison regressed in either
tier. The remaining 306 acceptance error differences are led by Rust-only
TS1182 and TS2339 and by native-only TS2430 and TS5023. E2 still does not pass,
and the divergence ledger remains empty.

Checks on the final tree: clean workspace formatting and clippy, 695 passing
Rust tests in debug and release, 442 passing script tests, `cargo xtask
validate`, and a current S07 operation inventory. Review confirmed two older,
unrelated divergences, recorded for separate fixes:
- destructuring-assignment accessibility errors span the whole property rather
  than its name;
- element-access self-type checks use the receiver's resolved symbol rather
  than the object type's symbol.

## Destructuring accessibility spans, S07 re-freeze and recorded evidence

Object-literal destructuring accessibility errors now use the property name
as the error node, matching the default case of `checkPropertyAccessibilityEx`.
The regression uses the pinned `compiler/destructuringAssignment_private.ts`
spans for identifier, shorthand and computed names.

Commit `2a70abc` refreshed three `operations.json` anchors without re-freezing
the S07 subset, so the E2 and program producers rejected the reviewed subset.
With owner approval:
- `data/s07/subset-review.json` gained finding
  `S08-P6-2026-09-14-syntactic-diagnostics-mappings` and the updated
  `subset-rule.json` candidate digest;
- `subset-rule.json` was re-frozen.

Only `rust_mappings` changed. Selected variants and checker obligations keep
their accepted hashes, and an independent check confirmed the field-level scope
and every recorded hash.

The final capture at `target/s08/e2` has corpus capture SHA-256
`f3d539156384f4a1a5cbf58063e36fdca98bd0ee4bf55dd66632ad318f57b18d`. The
preceding batch capture is kept at `target/s08/e2-batch2`. Final acceptance
results:
- errors parity 0.9675526 (9,065 of 9,369);
- types parity 0.9706479;
- public type-to-string parity 0.9773722.

Two more error comparisons match than in the batch capture:
`destructuringAssignment_private` and the TS2715 spans in
`abstractPropertyInConstructor`. Nothing regressed.

Evidence was recorded on 2026-09-15 from `2a70abc` with these uncommitted
changes. `e2`, `program`, `fmt`, `clippy`, `deny`, `selftest`, `workspace`,
`oracle`, `scanner`, `e4`, `checkertext`, `e1`, `binder` and `e3` are current.
`gen` stays stale because this host lacks the pinned generator Node v24.20.0;
`bindworkload`, `e5` and `e6` require the measurement host. The CI metric checks
pass except the four `gen` metrics, which are pending.

E1 and E4 pass. E2 fails for three reasons:
- neither parity metric is 1;
- divergences are unapproved;
- checker benchmark and relater evidence is missing.

E3's S08 checker contributors also remain missing.

## Element-access self references and generator evidence

`markPropertyAsReferenced` now receives the self-access parent the way pinned
Go computes it at each call site. Element access passes the apparent object
type's symbol, and property access passes the receiver's resolved symbol. The
containing-method search stops only at function-like declarations, so function
types and signatures inside a method no longer end it early. Native witnesses in
`data/s08/p6/element-self-access` cover:
- local, static, `this`, `this`-typed alias and static self receivers in
  exported and module-local classes;
- a type-query self access under a function type;
- a static alias receiver.

Before the change, Rust reported TS6133 for `self["m"]` and exported
`StaticSelf["m"]`, and omitted it for `AliasSelf.alias["m"]` and
`TypeQuerySelf`.

`gen` evidence was recorded with the pinned generator runtime from the local
`target/s03-runtime` bootstrap (Node 24.20.0, npm 11.17.0). All four CI
generator metrics pass. This checker change leaves the checker-dependent
evidence recorded before it stale until the next E2 capture and evidence round.

## Interface inheritance checks

The interface declaration path now checks inherited-property identity before
relating the derived type to each base. It preserves Go's once-per-merged-symbol
guard, polymorphic `this` substitution, diagnostic chains and check order.
Conflicting inherited properties suppress base relations and index constraints;
member and duplicate-index checks still run. Merged generic declarations use the
existing type-parameter-list identity check.

The bounded comparison exposed two connected omissions, now fixed:
- A merged class/interface symbol resolves its class base followed by its
  interface bases. Previously, returning after the class base dropped the
  interface bases and produced false missing-abstract-member diagnostics.
- Heritage grammar rejects `implements`, repeated `extends`, empty lists and
  trailing commas. Only the first `extends` clause supplies base types; the
  later clause's identifiers are not resolved as additional bases.

Twenty-five native programs in `data/s08/p6/interface-inheritance` compare full
diagnostic payloads, including chains and related information, on initial and
repeated semantic requests. The requests, native provenance and reproduction
commands are retained with the fixtures.

The 130-variant screen in
`target/s08/p6-interface-inheritance-recheck-02` includes all 33 acceptance
variants missing TS2430 or TS2320 in the recorded E2 control, plus neighboring
interface cases. Sources were stable throughout. Using the unchanged E2
comparators, with no approved divergences:

| Domain | Matching before | Matching after |
| --- | ---: | ---: |
| Types and symbols | 127 | 129 |
| Errors | 93 | 129 |
| Public type-to-string | 129 | 129 |

All 33 originally targeted variants now match in every domain. No previously
matching comparison regressed, and no variant failed execution. The remaining
`compiler/interfaceClassMerging` difference is unchanged: a missing TS2722 on an
optional-method call and a type/query difference. It remains a separate task.

`tools/s08/p6/interface-inheritance/results.json` retains the exact improved
IDs, remaining differences and capture fingerprints. This is a development
screen, not an updated full-corpus E2 result. The full E2 capture and its
checker-dependent evidence remain stale until the next coordinated capture.

Validation: 81 compiler checker-semantic regressions pass (including the 25
native programs), as do all 57 checker unit tests with all features. Formatting
and clippy for `ts_compiler` with all targets/features pass, and the S07
program-operation matrix validates without changes.

## Catch destructuring and speculative alias resolution

Catch declarations now call `checkVariableLikeDeclaration` directly, matching
Go's `checkCatchClause`. Ordinary variable declarations still call their grammar
check before the shared semantic checks. This removes TS1182 on catch patterns
without suppressing invalid catch annotations, initializers or redeclarations.
Ten captured native programs cover both paths; the regression failed with the
extra TS1182 before the fix.

Spelling suggestions now use `tryResolveAlias` and the first resolved target's
flags. Previously they used `getSymbolFlags`, which eagerly re-entered an active
alias resolution and marked the chain circular; its unknown-target `ALL` flags
also admitted inappropriate suggestion candidates. Exact name lookup retains
normal alias resolution. The speculative path skips an active alias without
changing its cache or the resolution stack's failure flags.

The read-only cycle probe and resolution pushes share the property-produced
predicate. It includes all ten property kinds from pinned Go, so a completed
intermediate type, signature or symbol stops the probe as upstream requires.
Fourteen native programs retain true self, mutual, namespace and cross-file
cycles alongside missing-name and valid-suggestion cases. A checker unit test
also proves that a speculative lookup leaves the entire active chain successful
and does not cache an unknown target.

The selected captures were stable throughout:

| Selection | Domain | Matching before | Matching after |
| --- | --- | ---: | ---: |
| Catch: 32 variants | Types and symbols | 29 | 31 |
| Catch: 32 variants | Errors | 21 | 32 |
| Catch: 32 variants | Public type-to-string | 31 | 31 |
| Alias: 44 variants | Types and symbols | 43 | 43 |
| Alias: 44 variants | Errors | 22 | 40 |
| Alias: 44 variants | Public type-to-string | 43 | 43 |

All 11 extra-TS1182 cases now match diagnostics. All 20 extra-TS2303 cases lose
the spurious cycles; 18 now match diagnostics completely. The two remaining
targets are `constructorWithIncompleteTypeAnnotation` (missing TS2591/TS7010) and
`parser519458` (missing TS2591). Two cycle controls,
`declarationEmitUnknownImport` and `declarationEmitUnknownImport2`, retain their
previous missing TS2304; their native TS2303 diagnostics remain present.
`implicitAnyInCatch` retains its prior type/display difference. No previously
passing domain regressed in either screen, and every selected variant completed.

Requests, native observations, provenance and reproduction commands are in
`data/s08/p6/catch-destructuring` and `data/s08/p6/alias-circularity`. The matching
`tools/s08/p6` directories retain selections and exact comparison results. Local
captures are `target/s08/p6-catch-recheck-01` and
`target/s08/p6-alias-recheck-01`. These are bounded development results; the
full E2 evidence remains stale until a coordinated capture.

Validation: all 83 compiler checker-semantic tests and 58 checker unit tests
with all features pass. Clippy for `ts_compiler` with all targets/features and
warnings denied is clean. No full corpus or CI rerun was needed for this change.

## JS open objects, function expandos and CommonJS bindings

Property and indexed access now apply pinned Go's `isJSLiteralType` rule after
ordinary property/index lookup. Unconstrained JS literal objects permit missing
properties when `noImplicitAny` is off; every union constituent must be open,
while one open intersection constituent suffices. Instantiable types use their
resolved base constraint. Known properties, contextual annotations, readonly
indexes, TypeScript objects, `keyof` and assignability retain their existing
rules. Openness does not synthesize an index signature or add members.

Function member resolution now reads `getExportsOfSymbol` rather than raw
exports, so assignments through const-string and unique-symbol keys are included.
The existing late-member resolver supplies its recursive early-table and failure
handling. CommonJS variable and binding declarations now take Go's alias-check
branch before ordinary variable checks; missing exports keep their alias
diagnostic without an additional destructuring-property error.

Twenty-two native programs in `data/s08/p6/js-object-expando` cover these paths
and closed/strict controls. The initial openness fixture failed before its fix;
the expanded function/alias cases failed before the subsequent fixes. Full
diagnostic payloads and repeat requests match the captured native results.
A checker unit regression also preserves missing-index lookup for standalone
checker owners without a Program; the new openness probe must not make those
existing type operations require a program host.

The final stable capture, `target/s08/p6-js-object-recheck-02`, includes all 209
acceptance variants under `conformance/salsa` or with `expando` in their name.
All executed. The unchanged E2 comparators, without divergences, report:

| Domain | Matching in recorded E2 control | Matching after |
| --- | ---: | ---: |
| Types and symbols | 167 | 181 |
| Errors | 184 | 205 |
| Public type-to-string | 175 | 180 |

No previously matching domain regressed. Of the 21 additional diagnostic matches,
one (`moduleExportAliasUnknown`) comes from the preceding alias-cycle commit;
20 are attributable to this work. The type/symbol and public-display gains are
14 and five respectively. The record retains that attribution instead of
counting the earlier alias fix twice.

The four remaining diagnostic mismatches in this broad selection are unchanged:

- `contextualTypedSpecialAssignment`: a related-information argument expands the
  JSDoc alias instead of naming `DoneStatus`.
- `globalMergeWithCommonJSAssignmentDeclaration`: the DOM/global merge produces
  a different assignment type (`1` to `never`, versus `number` to `string`).
- `plainJSRedeclare2`: the operator diagnostic uses literal rather than widened
  operand types (`1`/`false`, versus `number`/`boolean`).
- `plainJSGrammarErrors`: missing TS1013.

The selection also retains 28 type/symbol and 29 public-display differences;
this increment does not claim full Salsa or E2 parity. Exact remaining IDs and
domain hashes are in `tools/s08/p6/js-object-expando/results.json`. The earlier
openness-only screen and its hashes are retained in `openness-results.json`;
the final combined capture, rather than sums of those screens, supplies the
reported results. Reproduction commands are with the native fixtures.

Validation: all 84 compiler checker-semantic regressions and 59 checker unit tests
with all features pass. Clippy for `ts_compiler` with all targets/features and
warnings denied, formatting, ledger validation and committed status views pass.
Full-corpus E2 evidence remains stale until the next coordinated capture.

## Closure of the remaining JS property and expando selection

The thirty variants still different after `c86138e` were investigated from the
existing captured outputs, starting with all twelve failing
`typeFromPropertyAssignment*` variants. Their property accesses carried the
receiver's unresolved JSDoc alias into the result; native canonicalizes the
error type while preserving the alias on the receiver itself. The same omission
explained nested-class, namespace, parameter-tag and prototype cases. Public
and lexically resolved private accesses now follow the native branches.

The other captured differences led to the following changes:

| Native contract | Change |
| --- | --- |
| `getTypeForVariableLikeDeclaration` | Concrete `for…in` keys are `string`; only generic index/type-parameter keys use `Extract`. `getIndexTypeOrString` remains separate for checking an existing assignment target. |
| `getNonNullableTypeIfNeeded` | Both `for…in` paths test nullable facts before removing nullability. A new generic control caught unconditional narrowing of `T` to `T & {}`; that candidate was corrected before the final screen. |
| `getAliasSymbolForTypeNode` | Include reparsed `JSTypeAliasDeclaration`, preserving named JSDoc object/union aliases in types, diagnostics and display. |
| `getTypeOfFuncClassEnumModuleWorker` | Optional methods retain their optional type under strict null checks; non-strict methods remain unchanged. |
| Call inference contexts | Set `InferenceFlagsAnyDefault` for JS calls, including the fallback inference path. TS calls retain `unknown` when no inference is available. |
| `isReadonlyAssignmentDeclaration` | Inspect the cached `Object.defineProperty` descriptor, its `value`/`writable` properties and setter presence. Read the third argument without copying the argument list. |
| `resolveAnonymousTypeMembers` | Build `globalThis` members from runtime globals, excluding lexical declarations and ambient-only modules. Ordinary global name resolution still uses the original table. This removes the erroneous `string & void` result for `window.name`. |
| Addition diagnostic arguments | Apply the native base-type compatibility predicate before formatting incompatible operands. This fixes `plainJSRedeclare2` without widening every operator diagnostic indiscriminately. |
| `checkGrammarParameterList` | Report TS1013 on a trailing comma after a non-ambient final rest parameter, then continue its optionality/initializer checks in native order. |
| Signature serialization | Use the original parameter list when expansion would place a variadic before another parameter; fixed tuples still expand. This fixes the remaining `multipleDeclarations` public-display difference. |

These preserve checker ownership and existing cache lifetimes. Readonly checks
now require mutable checker access because descriptor evaluation can populate
expression/type caches. Normal non-call declarations allocate no descriptor
argument vector. Global member filtering creates one checker-owned member table
at resolution, without modifying bound source symbols or the globals table.

Thirteen independently captured native diagnostic programs cover these paths
and negative controls. Additional source regressions cover public/private error
propagation, JSDoc alias/optional-method display across operations, and variadic
versus fixed-tuple serialization. Requests, observations, provenance and commands
are in `data/s08/p6/js-property-followups`; selections and results are under the
matching `tools/s08/p6` directory.

The final optimized capture is `target/s08/p6-js-followups-recheck-01`, compared
with `target/s08/p6-js-object-recheck-02` using the unchanged E2 comparators and
no divergences. All 209 variants executed, and sources stayed stable:

| Domain | Matching before | Matching after |
| --- | ---: | ---: |
| Types and symbols | 181 / 209 | 209 / 209 |
| Errors | 205 / 209 | 209 / 209 |
| Public type-to-string | 180 / 209 | 209 / 209 |

All thirty previously different variants now match every domain. No previously
matching domain regressed. This closes the selected JS object/expando cases;
it does not update full-corpus E2 evidence or establish parity outside this
selection.

Validation: 88 compiler checker-semantic tests and 59 checker unit tests pass;
the extended private-access regression also passes after its final addition.
Clippy for `ts_compiler` with all targets/features and warnings denied is clean.
Formatting, ledger validation and committed tracker views are checked before
committing. No full-corpus capture or CI rerun was required.

## Full E2 at `827c353` and alias-accessibility correction

The complete capture `224d2fa6` (recorded E2 artifact
`5eb35a02a72217dfb080a804ae1927296efbc240dd2450c5e81c1777b9f79490`)
measured all 9,369 acceptance variants with no execution failures. Against
the preceding full capture, no previously matching domain became different:

| Domain | Different variants before | Different variants at `827c353` |
| --- | ---: | ---: |
| Errors | 304 | 162 |
| Types and symbols | 275 | 88 |
| Public type-to-string | 212 | 39 |

There are 218 distinct acceptance variants with at least one difference; 129
differ only in errors. The domain counts overlap. E2 still fails exact parity,
and no baseline divergences are approved.

That aggregate regression test did not catch a changed line inside an already
different variant. `conformance/jsdoc/typedefOnStatements` improved on nineteen
differences but started printing the local `alpha` as `Alpha`; pinned native
prints `{ alpha: string; }`. Retaining the JSDoc alias is correct. The missing
step was `NodeBuilderImpl.typeToTypeNodeWorker`'s alias-accessibility guard.
The builder now uses the alias only if explicitly allowed outside its scope or
if the existing symbol-accessibility worker accepts it in type meaning.

The JSDoc regression failed before the correction and passes afterward,
including repeated requests across operations. Twenty-four new native display
probes cover local, global and shadowed aliases through both public display
entry points, the enclosing declaration/source/absent contexts, and the explicit
out-of-scope flag. All 89 checker-semantic tests and six display tests pass;
compiler/dependency clippy with warnings denied, formatting and ledger
validation pass.

The stable development capture
`target/s08/p6-alias-accessibility-recheck-01` covers 503 acceptance variants:
all JSDoc cases, all previous public-display differences and the completed
209-case JS object/expando selection. Only `typedefOnStatements` changes, and
it now matches all three domains. Types/symbols improve from 462 to 463 matches
(two disabled); error and public-display results are unchanged. No matched
comparison regresses, and no already-different comparison changes except the
repaired one. Requests, native observations and replay instructions are in
[`data/s08/p6/alias-accessibility`](../data/s08/p6/alias-accessibility/README.md).
The result record is `tools/s08/p6/alias-accessibility/results.json`.

The next diagnostic candidates have concrete witnesses in the full capture:
the six `deprecatedCompilerOptions` variants miss seven TS5023 diagnostics each;
all seven variants with extra TS2411 counts involve the synthetic `prototype`
property and static index signatures. Missing TS1036 spans nine variants, and
missing TS7010 spans eight. These are attribution findings, not implemented
fixes. The correction above makes the full E2 evidence stale; the 503-case
screen does not publish replacement full-corpus ratios.

## Static-index prototype checks and original config diagnostics

The seven extra-TS2411 variants omitted the native static-side exception in
`Checker.checkIndexConstraints`. The checker now passes the static/instance
mode explicitly and skips symbols carrying `SymbolFlagsPrototype` only for
static indexes. Real properties named `prototype` remain subject to index
constraints. Seven independently captured native programs cover synthetic,
instance/interface, inherited and real static properties, and conflicting
string/numeric indexes. Their full diagnostic payloads and repeat operations
match.

The six `deprecatedCompilerOptions` fixtures exposed a separate harness gap.
At the pin each reports seven TS5023 unknown-option errors and one TS6046
invalid-target error. Production config parsing already recognized those
options; the baseline executor discarded the native runner's config phase
when constructing a Program from the frozen effective options and roots.

The executor now parses the original config from `error_inputs`, using the
existing tsoptions implementation and a shared S07 resolver host. It retains
config syntax owners, errors, inherited configs and include specifications
alongside the unchanged compiler input list and effective options. Native
`CompileFilesEx` constructs a fresh command line with zero-valued path-matching
settings, which the adapter also preserves. Root test-unit text is parsed
directly; filesystem BOM conversion applies only to inherited config reads.
`ParsedCommandLine.config_file_parsing_diagnostics` combines root syntax
diagnostics and option/conversion diagnostics in native order, and both P2
and P4/P5/E2 use that API. Diagnostic sorting, formatting and serialization now
resolve retained inherited config owners as well as the root config.

The first combined screen caught an omitted config-spec copy in
`tsconfigRootdirInclude`. Its regression then exposed the missing config syntax
diagnostic and incorrectly copied path-matching context. Those were corrected;
the final screen also checks the direct root-text path. Five config tests cover
the six native option fixtures, inherited source ownership, BOM retention,
missing-input failure and the exact malformed-config native diagnostics.

The capture review also found that Python's `Path.glob("directory/**")` was
omitting files under the P4/P5 adapter directories. Those source patterns now
use `**/*`; the shared host and P4/program adapters are also included in the
E2 ledger's sources. A regression checks the actual content hashes and ledger
coverage. The regenerated S07 operation inventory changes only three Rust
mapping line numbers.

The final optimized capture, `target/s08/p6-static-index-config-recheck-03`,
contains all 128 config-bearing variants and 17 static/index controls, with
stable sources and no execution failures. The unchanged E2 comparators report:

| Tier and domain | Before (`827c353`) | After |
| --- | ---: | ---: |
| Acceptance errors | 72 match, 21 different | 93 match |
| Acceptance types/symbols | 83 match, 9 disabled, 1 different | 84 match, 9 disabled |
| Acceptance public display | 84 match, 9 disabled | unchanged |
| Informational errors | 52 different | 52 match |
| Informational types/symbols and display | 43 match, 9 disabled | unchanged |

Every selected domain now matches or is natively disabled. No matched domain
regresses, and no selected already-different domain remains different. Besides
the thirteen targeted variants, retaining config metadata corrects eight more
acceptance error baselines and all 52 informational config error baselines.
`typingsLookup1` also gains type/symbol parity. This selection excludes the
earlier alias-accessibility fix, so that gain is not counted again.

Requests, native provenance, scope and reproduction commands are in
[`data/s08/p6/static-index-prototype`](../data/s08/p6/static-index-prototype/README.md)
and [`data/s08/p6/config-diagnostics`](../data/s08/p6/config-diagnostics/README.md).
`tools/s08/p6/static-index-prototype/results.json` records all changed IDs,
capture hashes and both preliminary screens. No full E2 ratios or gate claims
are inferred from these bounded results; the full evidence remains stale.

Validation: 97 focused compiler tests (90 checker-semantic, five config,
source-config parity and option verification), 21 script tests, compiler and
dependency clippy with all targets/features and warnings denied, formatting,
ledger validation and committed status views. No full-corpus or CI rerun.

## Error-parity tail at `9a52b85`

The full E2 capture at `9a52b85` (types 0.993, errors 0.985, public display
0.996) left 140 acceptance and 9 informational error differences. Their
diagnostics were grouped by missing, added and same-position payload
differences, and the groups were ported one Go path at a time:

- Ambient-context grammar: `checkGrammarStatementInAmbientContext` now runs
  for empty and expression statements (TS1036).
- Implicit any: `checkFunctionOrMethodDeclaration` tests `nodeIsMissing` on the
  body, so functions whose body is a zero-width missing block report TS7010.
- Index constraints: class members with non-bindable computed names are checked
  against instance/static index signatures (TS2411), via a `hasBindableName`
  port.
- Grammar errors that upstream suppresses in files with parse errors now go
  through `grammarErrorOnNode`: parameter lists, index signatures (with the
  trailing-comma check TS1025), named tuple members (TS5085-TS5087); a
  reparsed `?` from a JSDoc bracketed parameter no longer reports TS1015.
- Declarations outside blocks (TS1156) for type aliases and interfaces;
  private method signatures outside classes (TS18016); abstract methods with
  implementations (TS1245); named JSON imports under node16+ ESM (TS1544).
- Error display: `getTypeNamesForErrorDisplay` (context-sensitive value
  declarations, fully qualified names when they collide) is used by missing
  property, relation and equality operator errors; `reportOperatorError` and
  `getBaseTypesIfUnrelated` shape TS2367 with the await hint; tuple element
  lists regenerate colliding names like other type lists.
- Relater elaborations: the `Object` hint (TS2696), primitive/wrapper hints,
  exactOptionalPropertyTypes messages (TS2375/TS2379), private-name mismatch
  (TS18015), abstract constructor assignment (TS2517), incompatible `this`
  types (TS2685), the namespace-import origin hint (TS7038), the `export *`
  default related information, and static private names skipped in unmatched
  property checks and never inherited by derived constructors.
- Relation shape: discriminant matching relates without reporting; the
  type-variable constraint pre-check never reports, so a `this` constraint
  failure is elaborated once; `getUnmatchedProperty` no longer skips
  `prototype`; `isDiscriminantProperty` reads the partial union property so
  unions containing `undefined` narrow by discriminant.
- Aliases and modules: exported `import =` aliases and export specifiers
  resolve their first identifier through `getResolvedSymbol` (TS2304, TS2708);
  string-literal import names display quoted; JS importers of `export =`
  members get the require hint (TS2597); require-call aliases name their module
  in the JS type-import hint; side-effect imports are not emittable imports;
  backslash-relative ambient module names are relative (TS2436); rest tuple
  elements defer type-alias resolution only through non-array element types.
- Smaller ports: async return checks skip async generators, the Promise
  resolve arity hint (TS2810), circular return types name the assigned
  variable (TS7023), JSDoc `@extends` mismatch reads lazily parsed JSDoc
  (TS8023), and module kinds display as `Node18` in JSON import errors.

The bounded recheck `target/s08/p6-error-parity-tail-01` covers all 149
differing variants and 300 sampled error-matching acceptance controls:

| Tier and domain | Before (`9a52b85`) | After |
| --- | ---: | ---: |
| Acceptance errors | 300 match, 140 different | 415 match, 25 different |
| Acceptance types/symbols | 398 match, 31 different, 11 disabled | 410 match, 19 different, 11 disabled |
| Acceptance public display | 403 match, 26 different, 11 disabled | 412 match, 17 different, 11 disabled |
| Informational errors | 9 different | 8 match, 1 different |

No selected domain regresses; sources are stable and every case executes.
The 26 remaining error differences (listed in
[`tools/s08/p6/error-parity-tail/results.json`](../tools/s08/p6/error-parity-tail/results.json))
are: union construct signatures losing the abstract check (TS2511), the
`super` field access hint in JS (TS2855), the Node type-definition hint for
`module`/`require` names (TS2591), generator yield widening (TS7057) and the
generator `noImplicitReturns` unwrap (TS7030), the `esmModuleExports` family,
declaration-emit name accessibility (TS4023, TS2415, TS2416), the flow-loop
comparability error suppression, `import ns` of `export as namespace` modules,
nested dotted namespace value resolution, parameter property literal widening,
tuple contextual typing in overloads (TS2769), and five single-variant
display or elaboration differences.

Validation: 109 checker-semantic compiler tests (19 new), 58 checker unit
tests, checker/compiler/module clippy with all targets and features and
warnings denied, formatting, ledger validation and committed status views.
Full E2 was not re-run; the recorded evidence remains at `9a52b85`.

## Remaining error-parity cases after `a515aec`

All 26 remaining error differences above are fixed against the same pinned Go
observations. The fixes retain native control flow and error behavior:

- Abstract construction inspects every member of a union composite signature,
  rather than the composite flags inherited from its first signature (TS2511).
- `super` checks use the native class-instance-property predicate, including
  JS expando declarations and prototype/static exclusions (TS2855).
- Property access through an unresolved import alias marks the alias referenced
  before returning `any`, so malformed imports still check their `module`
  value and report the Node type-definition hint (TS2591).
- Yield contextual typing recognizes all types with the `Any` flag;
  `noImplicitReturns` unwraps generator/async return types before its test.
  Conditional expressions preserve the expression-check mode, preventing
  transient flow-loop inference from reporting comparability errors.
- Node20/NodeNext CommonJS usage of an ESM module resolves `module.exports`
  before the default export. Namespace exports count as syntactic defaults;
  exported alias meaning uses the resolved module flags.
- Late computed property names track accessibility in the declaration being
  emitted. Symbol-expression tracking uses its explicit enclosing declaration.
  Type normalization reads resolved members so computed members cannot vanish
  when selecting an equivalent base type (TS4023, TS2415, TS2416).
- Readonly parameter properties widen literal types. Contextual array element
  lookup includes index signatures. Global `SymbolConstructor` annotations
  receive the native unique-symbol compatibility treatment.
- Error elaboration shares the discriminant/private intersection cause,
  preserves native generalized type display, and uses the effective return
  expression for error locations. Ambient module augmentation attributes report
  TS1551. Missing zero-width function bodies infer `any`, as native does.

Two earlier bucket labels were imprecise: `exportAsNamespace5` exercises
`export * as default`, and `dottedModuleName` differs because of a missing
function body, not dotted-namespace value resolution.

The optimized recheck `target/s08/p6-error-parity-remaining-01` reruns the same
449 IDs as the prior control, including its 300 sampled error-matching controls:

| Tier and domain | Before (`a515aec`) | After |
| --- | ---: | ---: |
| Acceptance errors | 415 match, 25 different | 440 match |
| Acceptance types/symbols | 410 match, 19 different, 11 disabled | 427 match, 2 different, 11 disabled |
| Acceptance public display | 412 match, 17 different, 11 disabled | 427 match, 2 different, 11 disabled |
| Informational errors | 8 match, 1 different | 9 match |
| Informational types/symbols and display | 7 match, 2 different | 9 match |

Every case executes, sources are stable, and no selected domain regresses.
The two remaining type/display differences are
`abstractClassUnionInstantiation` (direct construction queries retain a
non-`any` result) and `noImplicitAnyDestructuringInPrivateMethod` (object-binding
display omits a trailing comma). Their baseline lines were compared across
native, control and candidate: changed lines improve; neither residual worsens.

The [result record and replay instructions](../tools/s08/p6/error-parity-remaining/README.md)
retain capture identities and all domain transitions. A new
`checker_error_tail` regression compares the exact native diagnostic payloads
for all 26 witnesses; all failed before the fixes and pass afterward.

Validation: 59 checker unit tests, 109 checker-semantic tests, the 26-case
diagnostic regression, checker/compiler clippy with all targets/features and
warnings denied, formatting, ledger validation and refreshed status views.
The S07 operation inventory was regenerated for moved port locations. Full E2
was not rerun: these bounded results do not update full-corpus ratios or pass
its gate, and source-bound evidence is stale after the implementation changes.

## Full E2 capture at `2b5cc4d`

A complete capture of `2b5cc4d` is at `target/s08/e2`, with corpus capture
SHA-256 `502cde3a73909927df34dc1b21fce1235458b1a15c2b35772d75c605b9e3894f`. It
replays through `cargo xtask run e2` as current evidence. The `9a52b85` capture
is retained at `target/s08/e2-d9dfe66`. Native observations are byte-identical
between the two captures.

| Acceptance result | `9a52b85` | `2b5cc4d` |
| --- | ---: | ---: |
| Errors parity | 0.9850571 (140 different) | 1 (0 different) |
| Types parity | 0.9926353 (69 different) | 0.9962643 (35 different) |
| Public type-to-string parity | 0.9958373 (39 different) | 0.9989327 (10 different) |
| Execution failures | 0 | 0 |

The 9 informational error differences also match. Every per-metric change is
different to match, and no line or query in a still-different row lost a native
match. The bounded `error-parity-remaining` recheck agrees with the full capture
on all 449 selected IDs.

The 35 remaining acceptance type differences group as follows:
- 13 missing symbols for `import.meta`;
- 8 missing symbols for `this` inside type queries;
- 4 missing symbols for `new.target`;
- 4 binding-pattern displays that drop a trailing comma;
- 2 constructions of unions with abstract signatures that keep a non-`any`
  type;
- 4 single-case display differences: a `"-1"` property name, `objectFreeze`,
  literal `extends` rows and an `in`-operator intersection.

The 10 display differences mirror the display-only cases. E2 still fails its
gate on types parity and the empty divergence ledger.

Commit `2b5cc4d` moved six S07 operation-inventory `rust_mappings` anchors
without re-freezing the reviewed subset. Review finding
`S08-P6-2026-09-15-error-parity-tail-mappings` records them. Only
`subset-rule.json` operation-matrix metadata changed; selected variants and
checker obligations keep their accepted hashes.

## Types and display tail at `2b5cc4d`

With errors parity at 1.0, the capture at `2b5cc4d` left 44 variants differing
in types/symbols or public display. Each group was traced to one Go path:

- `getSymbolAtLocation`: the `meta` of `import.meta` resolves to the `meta`
  member of a synthetic `ImportMetaExpression` type
  (`getGlobalImportMetaExpressionType`), the `target` of `new.target` to the
  symbol of `checkNewTargetMetaProperty`'s type, and a `this` identifier inside
  a type query (`isThisInTypeQuery`) takes the `ThisKeyword` path instead of
  ordinary name resolution.
- `getQuickTypeOfExpression`: the shortcut for `new` and call expressions now
  follows `getSingleSignature`, so a union callee is never short-circuited and
  the abstract-union error path yields `any`.
- `getIntersectionType`: `isEmptyAnonymousObjectType` resolves late-bound
  members before deciding a type literal is empty, so `{ [a]: number }`
  survives intersection with `Record<"d", unknown>` and the union keeps its
  denormalized origin for display.
- `getReturnTypeFromBody`: when the contextual signature is the function's own
  signature, the contextual type is the inferred return type, keeping `false`
  under `Object.freeze`.
- Node builder and printer: property names from name types follow
  `getPropertyNameNodeForSymbolFromNameType` (`"-1"` stays a string literal);
  the reuse visitor keeps list locations like `NodeVisitor.VisitNodes`; the
  printer ports `hasTrailingComma`, consulting the original node's list.
- `isPartOfTypeNode`: the port checks the kind range only and decides
  `ExpressionWithTypeArguments` by its heritage clause, so literals in a class
  `extends` clause are expressions and get type rows.

The bounded recheck `target/s08/p6-types-display-tail-01` covers all 44
differing variants and 300 sampled controls matching both domains:

| Tier and domain | Before (`2b5cc4d`) | After |
| --- | ---: | ---: |
| Acceptance types/symbols | 300 match, 35 different | 335 match |
| Acceptance public display | 325 match, 10 different | 335 match |
| Acceptance errors | 335 match | 335 match |
| Informational types/symbols | 9 different | 9 match |
| Informational public display | 9 match | 9 match |

All 344 comparisons match in every domain. The full E2 was not re-run after
this pass; if it reproduces the bounded result, types, errors and public
display parity are all exactly 1 for the acceptance tier.

Validation: 112 checker-semantic compiler tests (3 new), checker and printer
unit tests, checker/compiler/printer clippy with all targets and features and
warnings denied, formatting, ledger validation and committed status views.


## Full E2 capture at `adc2e51`

A complete capture of `adc2e51` is at `target/s08/e2`, with corpus capture
SHA-256 `6eaf8605bc39122da5cf218c1ac5375da58ebae2453b707bca8729b7d3bc43ff`. It
replays through `cargo xtask run e2` as current evidence. Native observations
are byte-identical to the `2b5cc4d` capture, and the S07 subset review needed no
amendment.

| Acceptance result | `2b5cc4d` | `adc2e51` |
| --- | ---: | ---: |
| Errors parity | 1 | 1 |
| Types parity | 0.9962643 (35 different) | 1 |
| Public type-to-string parity | 0.9989327 (10 different) | 1 |
| Execution failures | 0 | 0 |

The types-display tail closed all 54 remaining differences, including 9 in the
informational tier:
- symbols for `import.meta`, `new.target` and `this` in type queries;
- binding-pattern trailing commas;
- construction of unions with abstract signatures;
- four single-case displays.

No comparison regressed. The bounded `types-display-tail` recheck agrees with
the full capture on all 344 selected IDs in every domain. The only unaccepted
rows are 34 informational variants whose native run fails on removed options.

All E2 correctness criteria now pass, including `divergences_approved`, which
holds vacuously because no difference remains. E2 stays pending only on the
checker benchmark and relater prototype metrics, which have no producer runs yet.
