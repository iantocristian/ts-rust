"use strict";
let duplicate; let duplicate;
function strict(eval, arguments) { let yield; return delete eval; }
export default function() {}
export default class {}
const computed = { [Symbol.iterator]() {}, [Math.random()]: 1, get value() { return 1; }, set value(v) {} };
interface I { property: string; property: number; method(): void; method(x: number): void; }
