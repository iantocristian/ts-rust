# Owned-text repair v2 evidence

The archive retains immutable candidate source and binaries, complete reused-Go graph captures, the fixed diagnostic screen, exact corrected E3 evidence, final checks and inventory-repair validation with prior failures. No v1 phase observation is attributed to v2.

Recorded decision: Accepted as the next experimental control after review, corrected E3 and complete fixed screening; final S07 Go-relative gates remain open.

Go graph streams are copied byte-for-byte from the verified v1 capture; this graph diagnostic executes only Rust. The unchanged checked-binder capture and byte-identical previous raw evidence are referenced in the v1 archive. CP1 remains the comparison control.

Every extracted member is hashed before replay. Source commit6de7612 and frozen byte hashes bind validation to the candidate. Full workload bytes and Cargo trees are excluded.

After extracting review.tar.xz, replay without any compiler or workload:

```sh
python3 target/s07-bis/local-text-repair/package.py replay --manifest /absolute/path/to/archive.json --output /tmp/local-text-repair-replay
```

The output directory must not exist. Replay validates copied Go provenance and Rust graph parity, all fixed-screen rows and summaries, historical distances, local-scope selection, E3 stdout metrics and exact29/76 inventories.
